"""``python -m manosube_agent_civilization.cli`` -- the module entry point this package's own
one console-script/module entry point choice resolves to (``05_CLI/CLI_CONTRACT.md`` §2)."""

from .main import main

if __name__ == "__main__":
    main()
