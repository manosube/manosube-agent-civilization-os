# Schema Migration Policy v0.1

Migration is explicit, version-to-version, deterministic, evidence-backed, and append-only. A migration record preserves source and target schema IDs, before/after fingerprints, tool version, authority reference, evidence reference, and failure status. Invalid or partial output is quarantined; the prior canonical record remains authoritative. Contract weakening requires Human Constitutional Authority.
